{
  "name": "listify-backend",
  "version": "1.0.0",
  "description": "Backend API for Listify application",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "cross-env NODE_ENV=production node Backend/server.js",
    "dev": "cross-env NODE_ENV=development node Backend/server.js",
    "build": "npm install && npm install --prefix Frontend && npm run build --prefix Frontend"
  },
  "keywords": [
    "express",
    "mongodb",
    "api",
    "listify"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^8.0.3"
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "nodemon": "^3.0.2"
  }
}
